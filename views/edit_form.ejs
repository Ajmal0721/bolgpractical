<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Item</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
    background-color: black;
    color: blue;
}

h2, label, .btn-primary {
    color: blue;
}

.form-control, .form-control-file, .btn-primary {
    border-color: blue;
    color: blue;
    background-color: black;
}

.form-control:focus, .form-control-file:focus {
    box-shadow: 0 0 5px blue;
    border-color: blue;
    background-color: black;
    color: blue;
}

textarea.form-control {
    color: blue;
    background-color: black;
    border-color: blue;
}

textarea.form-control:focus {
    box-shadow: 0 0 5px blue;
    background-color: black;
    color: blue;
}

.btn-primary {
    background-color: blue;
    border-color: blue;
}

.btn-primary:hover {
    background-color: #004080; /* Darker blue */
    border-color: #004080;
}

    </style>
</head>
<body>
  <%- include("../partials/navbar.ejs") -%>
    <div class="container mt-5">
        <h2 class="mb-4">Add New Item</h2>
        <form action="/editData" method="post" enctype="multipart/form-data">
            <input type="hidden" name="id" value="<%= val._id%>">
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" class="form-control" id="title" value="<%= val.title %>" name="title">
            </div>
            <div class="form-group">
                <label for="author">Author</label>
                <input type="text" class="form-control" id="author" value="<%= val.author %>" name="author">
            </div>
            <div class="form-group">
                <label for="description">Description</label>
                <textarea class="form-control" id="description" name="dsc" rows="3"><%= val.dsc %></textarea>
            </div>
            <div class="form-group">
                <label for="file">File Upload</label>
                <input type="file" class="form-control-file" id="file" name="image" >
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
